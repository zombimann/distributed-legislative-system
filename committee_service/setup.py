#setup.py

from setuptools import setup, find_packages

setup(
    name='committee_service',
    version='1.0.0',
    packages=find_packages(),
    install_requires=[
        # List your dependencies here (without specifying the exact versions)
    ],
)
