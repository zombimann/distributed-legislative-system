# setup.py
from setuptools import setup, find_packages

setup(
    name='amendment_service',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Include the required dependencies here if they are different from the test dependencies in requirements.txt
    ],
)
